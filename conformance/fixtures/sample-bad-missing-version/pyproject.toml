[project]
name = "samplegood"
version = "0.1.0"
description = "A minimal reference smart tool for conformance testing."
requires-python = ">=3.10"

[project.scripts]
samplegood = "samplegood.cli:main"
